name: Hello World GPU
description: Prints if GPU is available

implementation:
  container:
    image: tensorflow/tensorflow:2.4.2-gpu

    command: [
      python, -c, "exec(\"import tensorflow as tf\nprint()\nprint('GPU is available: {}'.format(tf.test.is_gpu_available()))\nprint()\")"
    ]